services:
  - type: web
    name: auroracart-dashboard
    runtime: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dashboard:server --workers 2 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
