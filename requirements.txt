# Runtime dependencies for the dashboard (Deliverable A).
# The pinned versions live in pyproject.toml — this file installs the project
# itself so `pip install -r requirements.txt` (Render's build command) works.
-e .
